# Só o painel de controlo precisa disto — não o bot. As dependências do bot
# (python-telegram-bot, openai, etc.) continuam no requirements.txt da raiz,
# para uma instalação em servidor (systemd, sem interface) não puxar uma
# biblioteca de interface que nunca vai usar.

# Servidor web local que desenha o painel — Material Design pronto a usar,
# sem escrever HTML/CSS/JS.
nicegui>=2.18.0,<4.0.0

# Janela própria em vez de uma aba do navegador — só no Windows. Usa o
# WebView2 que já vem com o Edge, sem nada a instalar à parte. Fora do
# Windows dependeria de bibliotecas de sistema (GTK ou Qt) nem sempre
# presentes, por isso fica de fora — o painel.py cai sozinho para o
# navegador se este pacote não estiver instalado (ver MODO_NATIVO).
pywebview>=5.0,<6.0; sys_platform == "win32"
